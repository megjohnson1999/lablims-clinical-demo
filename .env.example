# Database Configuration
DATABASE_URL=postgres://username:password@localhost:5432/pathogen_db

# JWT Secret for Authentication
JWT_SECRET=your_jwt_secret_key

# App Configuration
PORT=5000
NODE_ENV=development
LOG_LEVEL=info

# Label Printer Configuration
LABEL_TEMPLATE=PathDiscCapLid.Lab

# ============================================================================
# LLM-Powered Protocol Extraction (Optional AI Feature)
# ============================================================================

# Enable/Disable AI-powered reagent extraction from protocol documents
# Set to 'true' to enable, 'false' or omit to disable (default: false)
LLM_EXTRACTION_ENABLED=false

# Anthropic Claude API Key (Required if LLM_EXTRACTION_ENABLED=true)
# Get your API key from: https://console.anthropic.com/
# Cost: ~$0.01-0.05 per protocol extraction
# ANTHROPIC_API_KEY=sk-ant-api03-...

# ============================================================================
# Railway Deployment Instructions
# ============================================================================
#
# For Railway deployment, set environment variables in Railway dashboard:
# 1. DATABASE_URL - Automatically provided by Railway PostgreSQL addon
# 2. JWT_SECRET - Set manually (use a long random string)
# 3. NODE_ENV - Set to 'production'
# 4. (Optional) LLM_EXTRACTION_ENABLED=true - To enable AI extraction
# 5. (Optional) ANTHROPIC_API_KEY - Your Claude API key
#
# Note: Railway automatically redeploys when environment variables change
